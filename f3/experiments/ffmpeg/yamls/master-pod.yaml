apiVersion: v1
kind: Pod
metadata:
  name: mr-master
  namespace: openwhisk
spec:
  nodeName: node-8
  containers:
  - name: test
    image: rishabh0202/mr-master:v5
    imagePullPolicy: IfNotPresent
    volumeMounts:
    - name: f3
      mountPath: /var/f3
    - name: ceph
      mountPath: /var/ceph
  volumes:
  - name: f3
    persistentVolumeClaim:
      claimName: f3-pvc
  - name: ceph
    persistentVolumeClaim:
      claimName: ceph-pvc
